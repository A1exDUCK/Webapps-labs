function formatCityInfo(townName, population, area) {
    return `Town ${townName} has population of ${population} and area ${area} square km.`;
}

console.log(formatCityInfo('Moscow', 12636312, 2561));
